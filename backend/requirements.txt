Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-JWT-Extended==4.6.0
Flask-Cors==4.0.1
PyJWT[crypto]==2.10.1  # verifies Supabase Auth's JWKS-signed access tokens, see
                       # app/services/supabase_auth_service.py; [crypto] pulls in
                       # `cryptography` for ES256/RS256 verification
psycopg2-binary==2.9.9
pydantic==2.9.2
python-dotenv==1.0.1
openai==2.53.0  # needs the Responses API (client.responses) for tool calling with
                # reasoning models like gpt-5.6-sol; 1.51.0 predates it
httpx==0.27.2  # known-good pin; not required by openai==2.53.0 specifically
pypdf==5.0.1
pgvector==0.3.5
Pillow==11.0.0  # downscaling student-uploaded problem screenshots before vision transcription
langfuse==4.14.3  # tracing for tutor_service's model/tool calls, see docs there
stripe>=15.3  # subscription checkout/portal/webhooks, see billing_service.py
pip-system-certs==5.3  # uses the OS certificate store instead of certifi's bundled
                        # list; without it, outbound HTTPS (e.g. to OpenAI) can fail
                        # with CERTIFICATE_VERIFY_FAILED on networks with TLS-inspecting
                        # proxies (VPNs, corporate networks) whose root CA is trusted by
                        # Windows but not bundled in certifi

pytest==8.3.3
pytest-flask==1.3.0
black==24.10.0
ruff==0.7.0
mypy==1.13.0
